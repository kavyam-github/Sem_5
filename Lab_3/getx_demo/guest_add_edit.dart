/*
import 'package:demo_project/utils/import_export.dart';

class GuestAddEdit extends StatelessWidget {
  GuestGetController guestGetController =Get.find();

 GuestAddEdit({super.key}){
   Get.put(guestGetController);
 }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          APPBAR_GETX_ADD_EDIT,
          style: TextStyle(fontSize: 50, color: Colors.grey),
        ),
      ),

    );
  }
}
*/
